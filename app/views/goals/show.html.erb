<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @goal.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @goal.content %>
</p>

<p>
  <strong>Category:</strong>
  <%= @goal.category_id %>
</p>

<p>
  <strong>Deadline:</strong>
  <%= @goal.deadline %>
</p>

<% unless @goal.user == current_user %>
<% end %>

  <% if @done.present? %>
    <%= link_to 'お気に入り解除する', done_path(@done), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする', dones_path, method: :post, class: 'btn btn-default' %>
  <% end %>


<%= link_to 'Edit', edit_goal_path(@goal) %> |
<%= link_to 'Back', goals_path %>
